# Copy to .env.local and fill in — same project, same values as the web
# app's .env.local (VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY), just with
# the EXPO_PUBLIC_ prefix Expo requires to expose a var to client code.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
